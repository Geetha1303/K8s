apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      component: backend
  template:
    metadata:
      labels:
        component: backend
    spec:
      containers:
        - name: backend
          image: naveen5025/backend:v3
          ports:
            - containerPort: 8080
          env:
            - name: PGUSER
              value: postgres
            - name: PGHOST
              value: postgres-backend.yml
            - name: PGPORT
              value: "5432"
            - name: PGDATABASE
              value: loizenaidb
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: pgpassword
                  key: PGPASSWORD
