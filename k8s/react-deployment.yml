apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-deployment
  labels:
    app: react
spec:
  selector:
    matchLabels:
      app: react
  template:
    metadata:
      labels:
        app: react
    spec:
      containers:
        - name: react
          image: naveen5035/frontend:v3
          imagePullPolicy: Always
          stdin: true
          ports:
            - containerPort: 3000
      restartPolicy: Always
